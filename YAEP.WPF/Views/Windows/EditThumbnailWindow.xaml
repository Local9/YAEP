<ui:FluentWindow
    x:Class="YAEP.Views.Windows.EditThumbnailWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:YAEP.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Edit Thumbnail"
    Width="500"
    Height="400"
    d:DataContext="{d:DesignInstance local:EditThumbnailWindow,IsDesignTimeCreatable=False}"
    d:DesignHeight="400"
    d:DesignWidth="500"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResize"
    Focusable="True"
    KeyboardNavigation.IsTabStop="False"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    mc:Ignorable="d">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <ui:TitleBar
            x:Name="TitleBar"
            Title="Edit Thumbnail"
            Grid.Row="0"
            CloseWindowByDoubleClickOnIcon="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/wpfui-icon-256.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <TextBlock
                    FontSize="20"
                    FontWeight="Medium"
                    Text="Edit Thumbnail Setting" />
                <TextBlock
                    Margin="0,8,0,0"
                    FontSize="12"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="Configure the size and opacity for this thumbnail window." />
                
                <!-- Window Title (Read-only) -->
                <TextBlock
                    Margin="0,24,0,0"
                    Text="Window Title:" />
                <Border
                    Margin="0,8,0,0"
                    Padding="8,4"
                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1">
                    <TextBlock
                        Text="{Binding ViewModel.EditingThumbnailWindowTitle, Mode=OneWay}" />
                </Border>
                
                <!-- Width -->
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center" Text="Width:" />
                    <Slider
                        Grid.Column="1"
                        Margin="0,0,8,0"
                        Minimum="192"
                        Maximum="960"
                        TickFrequency="1"
                        IsSnapToTickEnabled="True"
                        Value="{Binding ViewModel.EditingThumbnailWidth, Mode=TwoWay}" />
                    <TextBlock Grid.Column="2" VerticalAlignment="Center" MinWidth="50" TextAlignment="Right" Text="{Binding ViewModel.EditingThumbnailWidth, Mode=OneWay}" />
                </Grid>
                
                <!-- Height -->
                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center" Text="Height:" />
                    <Slider
                        Grid.Column="1"
                        Margin="0,0,8,0"
                        Minimum="108"
                        Maximum="540"
                        TickFrequency="1"
                        IsSnapToTickEnabled="True"
                        Value="{Binding ViewModel.EditingThumbnailHeight, Mode=TwoWay}" />
                    <TextBlock Grid.Column="2" VerticalAlignment="Center" MinWidth="50" TextAlignment="Right" Text="{Binding ViewModel.EditingThumbnailHeight, Mode=OneWay}" />
                </Grid>
                
                <!-- Opacity -->
                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center" Text="Opacity:" />
                    <Slider
                        Grid.Column="1"
                        Margin="0,0,8,0"
                        Minimum="0"
                        Maximum="1"
                        TickFrequency="0.01"
                        IsSnapToTickEnabled="True"
                        Value="{Binding ViewModel.EditingThumbnailOpacity, Mode=TwoWay}" />
                    <TextBlock Grid.Column="2" VerticalAlignment="Center" MinWidth="50" TextAlignment="Right" Text="{Binding ViewModel.EditingThumbnailOpacity, StringFormat='{}{0:P0}', Mode=OneWay}" />
                </Grid>
                
                <!-- Buttons -->
                <StackPanel
                    Margin="0,24,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
                    <ui:Button
                        Margin="0,0,8,0"
                        Command="{Binding ViewModel.SaveEditThumbnailSettingCommand, Mode=OneWay}"
                        Content="Save" />
                    <ui:Button
                        Command="{Binding ViewModel.CancelEditThumbnailSettingCommand, Mode=OneWay}"
                        Content="Cancel" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>

